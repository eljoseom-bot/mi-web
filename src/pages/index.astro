---
---

<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width" />
  <title>Agencia SEO Profesional</title>

  <style>
    html {
      scroll-behavior: smooth;
    }

    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background: #000000;
      color: #f8fafc;
    }

    /* ===== HERO ===== */
    .hero {
      text-align: center;
      padding: 120px 20px;
      background: radial-gradient(circle at top, rgba(56, 189, 248, 0.18), #000000 65%);
      animation: fadeDown 1s ease-out;
    }

    .hero h1 {
      font-size: 48px;
      margin-bottom: 10px;
      color: #cbd5e1;
    }

    .hero-image {
      width: min(460px, 100%);
      max-width: 560px;
      margin: 30px auto 0;
      display: block;
      border-radius: 28px;
      box-shadow: 0 12px 50px rgba(56, 189, 248, 0.18);
    }

    .hero p {
      opacity: 0.9;
      font-size: 18px;
      color: #e2e8f0;
    }

    /* BOTÓN PRO */
    .btn {
      display: inline-block;
      margin-top: 25px;
      padding: 14px 24px;
      background: #38bdf8;
      color: #000000;
      text-decoration: none;
      border-radius: 10px;
      font-weight: bold;
      transition: all 0.3s ease;
      box-shadow: 0 0 0 transparent;
    }

    .btn:hover {
      transform: scale(1.05);
      box-shadow: 0 0 24px rgba(56, 189, 248, 0.45);
    }

    /* ===== SECCIONES ===== */
    .section {
      padding: 80px 20px;
      text-align: center;
    }

    .section h2 {
      color: #7dd3fc;
    }

    .section p {
      color: #cbd5e1;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 20px;
      margin-top: 40px;
    }

    .card {
      background: #111111;
      padding: 25px;
      border: 1px solid #0ea5e9;
      border-radius: 14px;
      transition: transform 0.3s ease, background 0.3s ease, border-color 0.3s ease;
      opacity: 0;
      transform: translateY(30px);
      animation: fadeUp 0.8s ease forwards;
    }

    .card:hover {
      transform: translateY(-8px);
      background: #111827;
      border-color: #22d3ee;
    }

    .card h3 {
      color: #f8fafc;
    }

    .card p {
      color: #cbd5e1;
    }

    .card:nth-child(1) { animation-delay: 0.2s; }
    .card:nth-child(2) { animation-delay: 0.4s; }
    .card:nth-child(3) { animation-delay: 0.6s; }

    .sobre, .contacto {
      background: #050816;
      border: 1px solid #3b82f6;
    }

    footer {
      text-align: center;
      padding: 20px;
      background: #000000;
      font-size: 14px;
      color: #94a3b8;
    }

    /* ===== ANIMACIONES ===== */
    @keyframes fadeDown {
      from {
        opacity: 0;
        transform: translateY(-30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes fadeUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>

<body>

  <header class="hero">
    <h1>🚀 Consultor de SEO Profesional</h1>
    <p>Posicionamos tu negocio en Google y aumentamos tus clientes</p>

    <img class="hero-image" src="/seo-hero.svg" alt="Ilustración de servicio SEO" />

    <a href="/servicios" class="btn">
      Quiero mejorar mi SEO
    </a>
  </header>

  <section class="section">
    <h2>Servicios</h2>

    <div class="grid">
      <div class="card">
        <h3>📈 SEO On-Page</h3>
        <p>Optimización completa de tu web para Google.</p>
      </div>

      <div class="card">
        <h3>🔗 Link Building</h3>
        <p>Autoridad real con enlaces de calidad.</p>
      </div>

      <div class="card">
        <h3>🔍 Auditoría SEO</h3>
        <p>Detectamos todo lo que te está frenando.</p>
      </div>
    </div>
  </section>

  <section class="section sobre">
    <h2>¿Por qué nosotros?</h2>
    <p>Estrategias SEO reales que generan clientes, no promesas vacías.</p>
  </section>

  <section class="section contacto" id="contacto">
    <h2>Contacto</h2>
    <p>Te hacemos una auditoría gratuita</p>
    <a class="btn" href="mailto:contacto@tuweb.com">Enviar Email</a>
  </section>

  <footer>
    © 2026 Agencia SEO
  </footer>

</body>
</html>